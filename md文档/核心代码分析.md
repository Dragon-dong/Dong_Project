# 跨模态内容生成系统 - 核心代码分析

## 1. 主应用架构

### 1.1 FastAPI应用初始化
**所在文件**：`main.py`
**功能**：初始化FastAPI应用，配置CORS和静态文件服务
**核心代码**：
```python
# 初始化FastAPI应用
app = FastAPI(
    title="跨模态内容生成系统API",
    description="支持文生图、图生文、动态叙事生成、风格迁移的AI系统",
    version="1.0.0"
)

# 配置CORS，允许前端访问
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 在生产环境中应限制为特定域名
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 配置静态文件服务
app.mount("/static", StaticFiles(directory="."), name="static")
```
**实现说明**：
- 使用FastAPI框架创建Web服务，设置API标题、描述和版本
- 配置CORS中间件，允许跨域请求
- 挂载静态文件服务，提供前端页面访问

### 1.2 文生图API实现
**所在文件**：`main.py`
**功能**：实现文本生成图像的API接口，支持缓存和性能优化
**核心代码**：
```python
@app.post("/api/text-to-image")
async def text_to_image(request: TextToImageRequest):
    """文本生成图像接口"""
    start_time = time.time()
    try:
        # 构建缓存键
        cache_key = f"text_to_image:{request.prompt}:{request.style}:{request.custom_style}:{request.resolution}"
        
        # 尝试从缓存获取
        cached_result = db_ops.get_cache(cache_key)
        if cached_result:
            # 记录生成历史
            db_ops.create_generation_history(
                task_type="text_to_image",
                input_data=request.dict(),
                output_data=cached_result,
                status="success",
                duration=time.time() - start_time
            )
            return cached_result
        
        # 获取Stable Diffusion模型实例
        sd_model = get_sd_model()
        
        # 构建基础提示词
        base_prompt = request.prompt
        
        # 集成RAG模型增强风格提示词
        rag_model = get_rag_model()
        
        # 构建风格指令
        style_instruction = ""
        if request.style:
            style_instruction += f"{request.style}"
        if request.custom_style:
            if style_instruction:
                style_instruction += f", {request.custom_style}"
            else:
                style_instruction = request.custom_style
        
        # 使用RAG模型增强提示词
        if style_instruction:
            full_prompt = rag_model.enhance_style_prompt(base_prompt, style_instruction)
        else:
            full_prompt = base_prompt
        
        # 解析分辨率
        width, height = map(int, request.resolution.split("x"))
        
        # 生成图像（优化性能，减少推理步数）
        image = sd_model.generate_image(
            prompt=full_prompt,
            negative_prompt="low quality, blurry, distorted, ugly",
            width=width,
            height=height,
            num_inference_steps=15,  # 减少推理步数，提高速度
            guidance_scale=7.0  # 稍微降低引导尺度，提高速度
        )
        
        # 转换为Base64
        image_base64 = sd_model.image_to_base64(image)
        
        # 构建响应结果
        result = {
            "status": "success",
            "image_url": image_base64,  # 返回Base64编码的图像
            "prompt": request.prompt,
            "style": request.style,
            "custom_style": request.custom_style,
            "resolution": request.resolution,
            "enhanced_prompt": full_prompt  # 返回增强后的提示词
        }
        
        # 缓存结果（1小时）
        db_ops.set_cache(cache_key, result, expire=3600)
        
        # 记录生成历史
        db_ops.create_generation_history(
            task_type="text_to_image",
            input_data=request.dict(),
            output_data=result,
            status="success",
            duration=time.time() - start_time
        )
        
        return result
    except Exception as e:
        # 记录失败的生成历史
        db_ops.create_generation_history(
            task_type="text_to_image",
            input_data=request.dict(),
            output_data={},  # 空结果
            status="failed",
            error_message=str(e),
            duration=time.time() - start_time
        )
        raise HTTPException(status_code=500, detail=f"生成图像失败: {str(e)}")
```
**实现说明**：
- 使用缓存机制提高响应速度，避免重复生成
- 集成RAG模型增强提示词，提高生成质量
- 优化生成参数，减少推理步数，提高速度
- 记录生成历史，便于追踪和分析
- 提供错误处理，确保系统稳定性

## 2. 数据库操作实现

### 2.1 数据库操作类
**所在文件**：`database/db_operations.py`
**功能**：封装数据库操作，提供用户管理、生成历史管理、偏好设置和系统配置的CRUD操作
**核心代码**：
```python
class DatabaseOperations:
    """数据库操作封装类"""
    
    def __init__(self):
        """初始化数据库操作实例"""
        self.db = get_db_connection()
    
    # 用户管理操作
    def create_user(self, username: str, email: Optional[str], password_hash: str) -> Optional[int]:
        """创建新用户
        
        Args:
            username: 用户名
            email: 邮箱（可选）
            password_hash: 密码哈希值
            
        Returns:
            int: 用户ID，创建失败返回None
        """
        try:
            conn = self.db.get_mysql_connection()
            if not conn:
                return None
            
            # 确保email不为None，为空字符串
            email = email or ""
            
            with conn.cursor(dictionary=True) as cursor:
                sql = """
                INSERT INTO users (username, email, password_hash) 
                VALUES (%s, %s, %s)
                """
                cursor.execute(sql, (username, email, password_hash))
                conn.commit()
                return cursor.lastrowid
        except Exception as e:
            print(f"✗ 创建用户失败: {str(e)}")
            return None
```
**实现说明**：
- 初始化数据库连接
- 提供用户创建功能，处理异常情况
- 使用参数化查询，防止SQL注入
- 返回用户ID，便于后续操作

### 2.2 生成历史管理
**所在文件**：`database/db_operations.py`
**功能**：管理生成历史记录，支持创建、查询和删除操作
**核心代码**：
```python
def create_generation_history(
    self, 
    task_type: str, 
    input_data: Dict[str, Any], 
    output_data: Dict[str, Any], 
    status: str, 
    user_id: Optional[int] = None, 
    error_message: Optional[str] = None, 
    duration: Optional[float] = None
) -> Optional[int]:
    """创建生成历史记录
    
    Args:
        task_type: 任务类型
        input_data: 输入数据
        output_data: 输出数据
        status: 状态
        user_id: 用户ID（可选）
        error_message: 错误信息（可选）
        duration: 执行时长（可选）
        
    Returns:
        int: 记录ID，创建失败返回None
    """
    try:
        conn = self.db.get_mysql_connection()
        if not conn:
            return None
        
        # 转换数据为JSON字符串
        input_data_json = json.dumps(input_data, ensure_ascii=False)
        output_data_json = json.dumps(output_data, ensure_ascii=False)
        
        with conn.cursor(dictionary=True) as cursor:
            sql = """
            INSERT INTO generation_history 
            (user_id, task_type, input_data, output_data, status, error_message, duration) 
            VALUES (%s, %s, %s, %s, %s, %s, %s)
            """
            cursor.execute(sql, (
                user_id, task_type, input_data_json, output_data_json, 
                status, error_message, duration
            ))
            conn.commit()
            return cursor.lastrowid
    except Exception as e:
        print(f"✗ 创建生成历史失败: {str(e)}")
        return None
```
**实现说明**：
- 将输入和输出数据转换为JSON字符串存储
- 支持记录任务类型、状态、错误信息和执行时长
- 使用参数化查询，确保数据安全
- 返回记录ID，便于后续查询和管理

### 2.3 缓存操作
**所在文件**：`database/db_operations.py`
**功能**：提供Redis缓存操作，支持设置、获取和删除缓存
**核心代码**：
```python
def set_cache(self, key: str, value: Any, expire: Optional[int] = None) -> bool:
    """设置缓存
    
    Args:
        key: 缓存键
        value: 缓存值
        expire: 过期时间（秒，可选）
        
    Returns:
        bool: 设置是否成功
    """
    try:
        redis_client = self.db.get_redis_client()
        if not redis_client:
            return False
        
        # 转换值为JSON字符串
        value_json = json.dumps(value, ensure_ascii=False)
        
        if expire:
            redis_client.setex(key, expire, value_json)
        else:
            redis_client.set(key, value_json)
        
        return True
    except Exception as e:
        print(f"✗ 设置缓存失败: {str(e)}")
        return False
```
**实现说明**：
- 将缓存值转换为JSON字符串存储
- 支持设置过期时间，避免缓存无限增长
- 处理异常情况，确保系统稳定性
- 返回操作结果，便于调用方判断

## 3. 模型实现

### 3.1 Stable Diffusion模型
**所在文件**：`models/sd_model.py`
**功能**：封装Stable Diffusion模型，用于文生图功能
**核心代码**：
```python
class StableDiffusionModel:
    """Stable Diffusion模型封装，用于文生图功能"""
    
    def __init__(self, model_id: str = "runwayml/stable-diffusion-v1-5"):
        """初始化Stable Diffusion模型
        
        Args:
            model_id: 模型ID或本地路径
        """
        self.model_id = model_id
        self.pipeline = None
        self.device = "cuda" if torch.cuda.is_available() else "cpu"
        self.load_model()
        self.use_lightweight_model = True  # 使用轻量级模型提高速度
    
    def generate_image(
        self, 
        prompt: str, 
        negative_prompt: Optional[str] = None,
        width: int = 512,
        height: int = 512,
        num_inference_steps: int = 15,  # 减少推理步数，提高速度
        guidance_scale: float = 7.0,  # 稍微降低引导尺度，提高速度
        num_images_per_prompt: int = 1
    ) -> Image.Image:
        """生成图像
        
        Args:
            prompt: 描述文本
            negative_prompt: 负面提示词
            width: 图像宽度
            height: 图像高度
            num_inference_steps: 推理步数
            guidance_scale: 引导尺度
            num_images_per_prompt: 每提示生成图像数量
            
        Returns:
            生成的PIL图像对象
        """
        import time
        start_time = time.time()
        
        # 首先尝试使用SiliconFlow API生成图像
        try:
            print("✓ 尝试使用SiliconFlow API生成图像")
            
            # SiliconFlow API配置
            url = "https://api.siliconflow.cn/v1/images/generations"
            # 使用轻量级模型提高速度
            model_name = "stabilityai/sd-turbo" if self.use_lightweight_model else "Kwai-Kolors/Kolors"
            payload = {
                "model": model_name,
                "prompt": prompt,
                "image_size": f"{width}x{height}",
                "batch_size": num_images_per_prompt,
                "num_inference_steps": 1 if self.use_lightweight_model else num_inference_steps,  # SD Turbo只需要1步
                "guidance_scale": 0.0 if self.use_lightweight_model else guidance_scale  # SD Turbo不需要引导
            }
            headers = {
                "Authorization": "Bearer sk-ikomatcbxqwpaljmpcyxasoryvyneoaqbfphyclfmukltfhm",
                "Content-Type": "application/json"
            }
            
            # 调用API
            response = requests.request("POST", url, json=payload, headers=headers)
            response.raise_for_status()  # 检查请求是否成功
            
            # 处理响应
            data = response.json()
            image_url = data["images"][0]["url"]
            
            # 下载图像
            image_response = requests.get(image_url)
            from PIL import Image
            image = Image.open(io.BytesIO(image_response.content))
            
            # 保存图像到img文件夹
            import os
            if not os.path.exists('img'):
                os.makedirs('img')
            
            # 生成唯一的文件名
            import time
            timestamp = int(time.time())
            filename = f"img/generated_{timestamp}.png"
            image.save(filename)
            print(f"✓ 图像已保存到: {filename}")
            
            end_time = time.time()
            print(f"✓ SiliconFlow API生成成功，耗时: {end_time - start_time:.2f}秒")
            return image
            
        except Exception as e:
            print(f"✗ SiliconFlow API调用失败: {str(e)}")
            print("⚠️  尝试使用本地模型生成图像")
        
        # 如果API调用失败，尝试使用本地模型
        if self.model_loaded:
            try:
                # 生成图像
                images = self.pipeline(
                    prompt=prompt,
                    negative_prompt=negative_prompt,
                    width=width,
                    height=height,
                    num_inference_steps=num_inference_steps,
                    guidance_scale=guidance_scale,
                    num_images_per_prompt=num_images_per_prompt,
                    # 性能优化参数
                    callback=None,
                    callback_steps=1,
                    eta=0.0  # 确定性采样，提高速度
                ).images
                
                end_time = time.time()
                print(f"✓ 本地模型生成成功，耗时: {end_time - start_time:.2f}秒")
                return images[0]  # 返回第一张图像
            except Exception as e:
                print(f"✗ 本地模型生成失败: {str(e)}")
        
        # 如果所有方法都失败，返回占位图像
        from PIL import Image, ImageDraw, ImageFont
        image = Image.new('RGB', (width, height), color=(240, 240, 240))
        draw = ImageDraw.Draw(image)
        
        try:
            font = ImageFont.truetype("arial.ttf", 20)
        except:
            font = ImageFont.load_default()
        
        # 分别绘制两行文本
        line1 = "生成失败"
        line2 = "所有生成方法都失败了"
        
        # 计算每行文本的宽度
        line1_width = draw.textlength(line1, font=font)
        line2_width = draw.textlength(line2, font=font)
        
        # 绘制文本（居中）
        draw.text(((width - line1_width) // 2, height // 2 - 20), line1, fill=(100, 100, 100), font=font)
        draw.text(((width - line2_width) // 2, height // 2 + 10), line2, fill=(100, 100, 100), font=font)
        
        draw.rectangle([(10, 10), (width-10, height-10)], outline=(150, 150, 150), width=2)
        
        end_time = time.time()
        print(f"✗ 失败处理时间: {end_time - start_time:.2f}秒")
        return image
```
**实现说明**：
- 优先使用SiliconFlow API生成图像，提高速度
- 使用轻量级模型（SD Turbo）减少推理步数
- 失败时回退到本地模型，确保系统可用性
- 生成失败时返回占位图像，提供良好的用户体验
- 保存生成的图像到本地，便于后续使用

### 3.2 动态叙事生成
**所在文件**：`main.py`
**功能**：根据关键词生成连贯的故事和场景图像
**核心代码**：
```python
@app.post("/api/generate-story")
async def generate_story(request: StoryGenerateRequest):
    """动态叙事生成接口"""
    start_time = time.time()
    try:
        # 构建缓存键
        cache_key = f"generate_story:{request.keywords}:{request.story_style}:{request.story_length}"
        
        # 尝试从缓存获取
        cached_result = db_ops.get_cache(cache_key)
        if cached_result:
            # 记录生成历史
            db_ops.create_generation_history(
                task_type="generate_story",
                input_data=request.dict(),
                output_data=cached_result,
                status="success",
                duration=time.time() - start_time
            )
            return cached_result
        
        # 获取LLM模型实例
        llm_model = get_llm_model()
        
        # 获取Stable Diffusion模型实例
        sd_model = get_sd_model()
        
        # 生成故事场景
        print(f"开始生成动态叙事，关键词: {request.keywords}, 风格: {request.story_style}")
        scenes = llm_model.generate_story(
            keywords=request.keywords,
            story_style=request.story_style,
            story_length=request.story_length
        )
        
        # 为每个场景生成图像
        story_items = []
        for i, scene in enumerate(scenes):
            caption = scene.get("caption", "")
            
            # 生成场景图像
            print(f"生成场景{i+1}图像: {caption[:50]}...")
            
            # 构建图像生成提示词
            image_prompt = f"{caption}, {request.story_style} style, detailed, vivid, high quality"
            
            try:
                # 生成图像（优化参数提高速度）
                image = sd_model.generate_image(
                    prompt=image_prompt,
                    negative_prompt="low quality, blurry, distorted, ugly",
                    width=384,  # 降低分辨率提高速度
                    height=384,
                    num_inference_steps=10,  # 进一步减少推理步数
                    guidance_scale=7.0  # 降低引导尺度
                )
                
                # 转换为Base64
                image_base64 = sd_model.image_to_base64(image)
                
                # 添加到故事项
                story_items.append({
                    "image": image_base64,
                    "caption": caption,
                    "audio": f"audio{i+1}.mp3"  # 占位音频
                })
                
            except Exception as img_error:
                print(f"✗ 生成场景{i+1}图像失败: {str(img_error)}")
                # 生成失败时使用占位图像
                story_items.append({
                    "image": f"https://picsum.photos/seed/story{i+1}/512/512",
                    "caption": caption,
                    "audio": f"audio{i+1}.mp3"
                })
        
        # 构建响应结果
        result = {
            "status": "success",
            "story_items": story_items,
            "keywords": request.keywords,
            "story_style": request.story_style,
            "story_length": request.story_length
        }
        
        # 缓存结果（1小时）
        db_ops.set_cache(cache_key, result, expire=3600)
        
        # 记录生成历史
        db_ops.create_generation_history(
            task_type="generate_story",
            input_data=request.dict(),
            output_data=result,
            status="success",
            duration=time.time() - start_time
        )
        
        print(f"动态叙事生成完成，共{len(story_items)}个场景，耗时: {time.time() - start_time:.2f}秒")
        
        return result
    except Exception as e:
        # 记录失败的生成历史
        db_ops.create_generation_history(
            task_type="generate_story",
            input_data=request.dict(),
            output_data={},  # 空结果
            status="failed",
            error_message=str(e),
            duration=time.time() - start_time
        )
        raise HTTPException(status_code=500, detail=f"生成故事失败: {str(e)}")
```
**实现说明**：
- 使用LLM模型生成故事场景，Stable Diffusion生成对应图像
- 优化图像生成参数，降低分辨率和推理步数，提高速度
- 提供占位图像和音频，确保故事完整性
- 缓存结果，提高重复请求的响应速度
- 记录生成历史，便于后续分析和优化

## 4. 系统优化

### 4.1 性能优化策略
**所在文件**：`main.py` 和 `models/sd_model.py`
**功能**：优化系统性能，提高响应速度
**核心优化点**：
1. **缓存机制**：使用Redis缓存生成结果，避免重复计算
2. **API集成**：优先使用SiliconFlow API，提高生成速度
3. **参数优化**：
   - 减少推理步数（从默认的50步减少到10-15步）
   - 降低引导尺度（从默认的7.5降低到7.0）
   - 降低图像分辨率（从512x512降低到384x384）
4. **模型选择**：使用轻量级模型（SD Turbo），只需1步推理
5. **容错机制**：API调用失败时回退到本地模型，本地模型失败时返回占位图像

### 4.2 缓存实现
**所在文件**：`database/db_operations.py`
**功能**：提供高效的缓存机制，提高系统响应速度
**核心代码**：
```python
def get_cache(self, key: str) -> Optional[Any]:
    """获取缓存
    
    Args:
        key: 缓存键
        
    Returns:
        Any: 缓存值，未找到返回None
    """
    try:
        redis_client = self.db.get_redis_client()
        if not redis_client:
            return None
        
        value_json = redis_client.get(key)
        if value_json:
            return json.loads(value_json)
        return None
    except Exception as e:
        print(f"✗ 获取缓存失败: {str(e)}")
        return None
```
**实现说明**：
- 从Redis获取缓存值
- 将JSON字符串转换为Python对象
- 处理异常情况，确保系统稳定性
- 未找到缓存时返回None，便于调用方判断

## 5. 总结

本项目的核心代码实现了一个功能完整、性能优化的跨模态内容生成系统，主要包括：

1. **主应用架构**：基于FastAPI的Web服务，提供RESTful API接口
2. **数据库操作**：封装MySQL和Redis操作，提供数据存储和缓存功能
3. **模型集成**：集成多种AI模型，支持文生图、图生文、动态叙事生成、风格迁移和多语言文化适配
4. **性能优化**：通过缓存机制、API集成和参数优化，显著提高系统响应速度
5. **容错机制**：提供多层容错，确保系统稳定性和可用性

系统的核心价值在于：
- 集成多种先进AI模型，实现多模态内容生成
- 优化性能，提供快速响应的用户体验
- 支持多种应用场景，满足不同用户需求
- 代码结构清晰，易于维护和扩展

通过这些核心代码的实现，系统能够为用户提供高质量、快速的跨模态内容生成服务，具有广阔的应用前景。